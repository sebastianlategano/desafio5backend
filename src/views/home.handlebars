<div>
    <div>
        <p>Lista de productos (GET HTML):</p>
    </div>
    <div>
        <ul>
            {{#each products}}
            <li>{{title}} ${{price}}</li>
            {{/each}}
        </ul>
    </div>
</div>

<script src="js/index.js" ></script>